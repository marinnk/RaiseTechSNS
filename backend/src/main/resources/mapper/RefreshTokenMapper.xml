<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.raisetechsns.backend.mapper.RefreshTokenMapper">

    <sql id="columns">
        id, user_id, token_hash, expires_at, revoked_at, created_at
    </sql>

    <select id="findByTokenHash" resultType="com.raisetechsns.backend.entity.RefreshToken">
        SELECT <include refid="columns"/>
        FROM refresh_tokens
        WHERE token_hash = #{tokenHash}
    </select>

    <!--
        id・created_atはDB側（自動採番・DEFAULT now()）に任せるため、INSERT文には含めない。
        useGeneratedKeys/keyPropertyにより、採番されたidが実行後にrefreshToken.idへ反映される。
    -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO refresh_tokens (user_id, token_hash, expires_at)
        VALUES (#{userId}, #{tokenHash}, #{expiresAt})
    </insert>

    <update id="revoke">
        UPDATE refresh_tokens
        SET revoked_at = now()
        WHERE id = #{id}
    </update>

    <update id="revokeAllForUser">
        UPDATE refresh_tokens
        SET revoked_at = now()
        WHERE user_id = #{userId}
          AND revoked_at IS NULL
    </update>

</mapper>
