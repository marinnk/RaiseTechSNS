<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.raisetechsns.backend.mapper.UserMapper">

    <sql id="columns">
        id, username, email, password_hash, display_name, bio, avatar_url, created_at, updated_at
    </sql>

    <select id="findById" resultType="com.raisetechsns.backend.entity.User">
        SELECT <include refid="columns"/>
        FROM users
        WHERE id = #{id}
    </select>

    <select id="findByEmail" resultType="com.raisetechsns.backend.entity.User">
        SELECT <include refid="columns"/>
        FROM users
        WHERE email = #{email}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT EXISTS (SELECT 1 FROM users WHERE email = #{email})
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT EXISTS (SELECT 1 FROM users WHERE username = #{username})
    </select>

    <!--
        id・created_at・updated_atはDB側（自動採番・DEFAULT now()）に任せるため、
        INSERT文には含めない。useGeneratedKeys/keyPropertyにより、
        採番されたidが実行後にuser.idへ反映される。
    -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, password_hash, display_name)
        VALUES (#{username}, #{email}, #{passwordHash}, #{displayName})
    </insert>

</mapper>
